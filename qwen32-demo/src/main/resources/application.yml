spring:
  application:
    name: qwen32-demo
  ai:
    dashscope:
      #
      api-key: sk-xxxxx # 示例密钥，运行前请替换为真实 Key
      chat:
        options:
          top-k: 1
          top-p: 1
          model: qwq-plus
server:
  port: 8082
